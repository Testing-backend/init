/**
 * Bonus: optional on-chain loan + attestation writes.
 * Default BLOCKCHAIN_MODE=simulation — fake tx hashes are enough for the Chain page.
 */

export async function getChainStatusForUser(_userId: string): Promise<unknown> {
  throw new Error('TODO: implement getChainStatusForUser');
}
